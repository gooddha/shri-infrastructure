describe('Another Test Case', () => {
  it('Should return 2', () => {
    expect(1 + 1).toBe(2);
  });
});

describe('Third Test Case', () => {
  it('Should return 3', () => {
    expect(1 + 2).toBe(3);
  });
});